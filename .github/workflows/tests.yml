name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Tests
        uses: actions/setup-node@v1
        with:
          node-version: "14"
      - run: yarn install
      - run: yarn test
